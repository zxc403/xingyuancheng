// src/ai/Pathfinder.js
// v6.10.0 占位文件 - A* 寻路（v6.10.5 实现）

export class Pathfinder {
    constructor(navMesh) {
        this.navMesh = navMesh;  // 由 buildNavMesh() 构建
        console.log('[Pathfinder] 占位构造完成');
    }

    findPath(start, end) {
        // TODO v6.10.5: A* / NavMesh 寻路
        return [];
    }
}
